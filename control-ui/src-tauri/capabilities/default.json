{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the control UI window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
